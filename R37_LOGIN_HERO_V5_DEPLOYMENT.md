# Edusentia r37 Login Hero v5 Deployment

Production visual correction marker.

- Self-contained campus hero is delivered inside `r37-login-hero-inline-v5.css`.
- `index.html` activates `r37-final-ui-5` and the inline hero stylesheet.
- Service-worker cache generation is `v7-4-0-r37-edusentia-final-ui-5`.
- The login hero no longer depends on the external WebP asset request.
- Authentication IDs, handlers, MFA and licence bindings are unchanged.

This marker exists to force and identify the final GitHub Pages deployment containing the complete v5 visual correction.
